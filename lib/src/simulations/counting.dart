import 'package:flutter/material.dart';


class CountingHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Counting'),
        backgroundColor: Colors.red[500],
      ),
      backgroundColor: Colors.greenAccent,
    );
  }
}